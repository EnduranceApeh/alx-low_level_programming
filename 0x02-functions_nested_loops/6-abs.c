#include "main.h"

/**
 * _abs - compute the absoulute value of an integer
 * @n: expect an int as arguement
 * Return: return the absolute value of a number
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
